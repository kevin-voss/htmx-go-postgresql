APP_ENV=development
APP_ADDRESS=:8080
DATABASE_URL=postgres://forgeboard:forgeboard@localhost:5432/forgeboard?sslmode=disable
SMTP_HOST=localhost
SMTP_PORT=1025

# Local demo account from `make seed` (dev only — never use in production):
#   email:    demo@forgeboard.local
#   password: demo-password
#   workspace: /w/demo
